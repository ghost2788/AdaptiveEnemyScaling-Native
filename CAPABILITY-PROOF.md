# Native Capability Proof Register

Only a **Verified locally** capability may become a dependency of the full POC implementation. A documented or unsupported claim first receives an isolated acceptance spike. A rejected mandatory capability stops the native path.

| Capability | Classification | Evidence or required proof |
|---|---|---|
| Native Story signatures for `CombatIsActive`, `SavegameLoaded`, `GameModeStarted`, `EnteredCombat`, and `SwitchedCombat` exist in the installed game headers | **Verified locally** | Read-only local `story_header.div` inspection |
| Native status resources can express flat maximum HP, Attack, Saving Throw, AC, Spell DC, ActionPoint, and BonusActionPoint boosts | **Verified locally** | Read-only vanilla and installed native-mod package inspection |
| A native package can implement combat scaling without a Script Extender tree | **Verified locally** | Installed Tactician Enhanced package has native Story/Stats content and no Script Extender tree; its content is not a reuse source |
| Official Toolkit executable is installed on this machine | **Verified locally** | Steam tool app `2934770`, build `19988805`; manifest and `Glasses.exe` verified under `B:\SteamLibrary` on 2026-08-31; hashes recorded in `evidence/toolkit-install.json` |
| BG3 Toolkit Data DLC is enabled and locally complete | **Verified locally** | DLC app `2956320`, depot `2330358`, manifest `7444994150796633975`; BG3 manifest entry and `Data\Editor` verified on 2026-08-31 |
| Toolkit-generated module identity `a4567f52-1665-df50-b84c-3992f80fdb90` opens as an isolated project | **Verified locally** | Toolkit generated module UUID and folder opened successfully; project UUID is `58e90e45-f96d-379d-a71e-dbe0b8f36770`; evidence recorded in `evidence/toolkit-project-identity.json` |
| A Toolkit-generated module can be reassigned the reserved UUID `bb8bdf43-775b-4451-9ffd-69b5f3f531e8` by editing metadata and folder names | **Rejected** | The reassigned identity crashed Toolkit project scanning in `CoreLib.dll`; reverting to the generated identity restored successful opening. The generated identity was explicitly approved on 2026-08-31. |
| Toolkit Project Settings accepts a `0.1.0` module version | **Rejected** | Toolkit `4.1.1.6931813` enforces a minimum major version of `1`; the initial saved module was `1.0.0.0`, the first Publish Local produced `1.0.0.1`, and the accepted beta release candidate is `1.0.0.4`. The repository retains `0.1.0` only as its internal POC milestone. |
| Independently authored `AESN_` Story goals compile in the isolated native project | **Verified locally** | Toolkit generated `story.div` containing `DB_AESN_SchemaVersion(1)`, `PROC_AESN_TestApplyOneHp`, and `PROC_AESN_TestRemoveOneHp`; final compiler result was `0 error(s), 0 warning(s)` on 2026-08-31. |
| Publish Local can create a native `.pak` from the isolated project | **Verified locally** | Toolkit created a `22,508,559`-byte package with SHA-256 `4248C2D5840E1E3F39EF5AFDEDE0BF4A4B0454F31AD4DD2CF583116BDE54652F`. |
| Publish Local can save directly under repository `artifacts/` without writing the live player Mods directory | **Rejected** | The local run created `AdaptiveEnemyScalingNativePOC_a4567f52-1665-df50-b84c-3992f80fdb90.pak` in the live Mods directory. It was moved to ignored `B:` artifact storage and the live manifest was restored exactly; see `evidence/capability-spikes/CAP-02-publish-local.md`. |
| Publish Local performs no upload | **Documented but unverified locally** | Official Toolkit documentation; no authentication or Publish action was used, but network non-upload was not independently instrumented. |
| The 100 ms roster finalization barrier runs and an empty eligible roster fails closed | **Verified locally** | CAP-03 blank editor fixture launched and completed the combat-keyed timer, recorded size `0`, and created no combat snapshot |
| A host-controlled solo member derived through vanilla `DB_Players -> DB_PartyMembers` is captured once with its level and integer average | **Verified locally** | CAP-03 managed Shadowheart fixture recorded `size=1`, `sum=1`, `average=1`, and source `DB_PartyMembers`; see `evidence/capability-spikes/CAP-03-roster.md` |
| `MakePlayer(Gale,Shadowheart,1)` safely converts the off-level origin to the host user and joins the party | **Verified locally** | The isolated CAP-03 probe emitted `CharacterMadePlayer` and `CharacterJoinedParty`, assigned Gale user ID `65537`, and reported neither summon nor follower; it did not itself create a positive roster database fact |
| Vanilla Story derives `DB_PartyMembers` for the converted active Gale fixture from `DB_Players` | **Verified locally** | The isolated CAP-03 registry probe added only `DB_Players(Gale)`; vanilla Story inserted `DB_GLO_Playable(Gale)` and `DB_PartyMembers(Gale)` |
| The approved roster queries include an active origin companion and finalize exact two-member arithmetic | **Verified locally** | Shadowheart and Gale were both eligible with undefined gameplay owner; snapshot `size=2,sum=2,average=1,partyPercent=120`; see CAP-03 |
| The approved roster queries finalize an exact three-character host-controlled active roster | **Verified locally** | Shadowheart, Gale, and Astarion were each eligible; snapshot `size=3,sum=3,average=1,partyPercent=140`. This does not prove three network clients; see CAP-03 |
| The managed host fixture can be registered in vanilla `DB_Avatars` without invoking hireling logic | **Verified locally** | The isolated command added exactly `DB_Avatars(Shadowheart)`; no subsequent hireling, party-join, or player-conversion event occurred |
| Larian's built-in editor hireling path creates and registers an active hireling candidate | **Verified locally** | `hr_hire` completed for the Ranger global; vanilla Story added `DB_Players` and derived `DB_PartyMembers`, requested initial level, and emitted player/party events |
| Active hirelings pass the approved AESN roster exclusions and are counted once each | **Verified locally** | Retained Ranger and newly created Wizard each came from `DB_PartyMembers`, returned summon `0`, undefined owner, follower `0`, level `1`, and produced one snapshot-member fact; final snapshot `size=3,sum=3,average=1` with Shadowheart |
| A native familiar/summon present in `DB_PartyMembers` is excluded without changing the positive candidate source | **Verified locally** | Native Scratch returned `IsSummon=1` and owner Shadowheart; AESN recorded `Summon` and `Owned` exclusions, created no snapshot-member fact for Scratch, and finalized the host-only snapshot at `size=1` |
| A native temporary party follower present in `DB_PartyMembers` is excluded without changing the positive candidate source | **Verified locally** | A native kobold added through `AddPartyFollower` returned `IsSummon=0`, `IsPartyFollower=1`, and owner Lae'zel; AESN recorded `Owned` and `PartyFollower`, admitted only Lae'zel, and finalized `size=1` |
| `SetLevel` can safely synthesize a supported policy level on an off-level editor origin | **Rejected** | `SetLevel(Shadowheart,6)` crashed the Toolkit with a missing Experience Component and an access violation in `esv::Character::GetTotalXP`; the combined fixture was retired; see CAP-03 |
| `DB_PartyMembers` plus the approved exclusions produces the exact eligible roster | **Assumption/unsupported** | CAP-03 |
| One independently authored `IncreaseMaxHP(1)` status applies, acknowledges, preserves living HP percentage once, and removes with exact maxima | **Verified locally** | CAP-04 living trace: `12/12 -> 13/13 -> 12/12`, exact `StatusApplied`/`StatusRemoved`, 100% preserved, bit `1`; see `evidence/capability-spikes/CAP-04-flat-hp.md` |
| Flat-HP processing cannot resurrect a dead target | **Verified locally** | Native status application to a dead character is rejected; the implemented policy detects `0` HP before mutation, applies no bit, performs no percentage write, preserves `0/12`, and retires the target; see CAP-04 |
| Multiple binary flat-HP statuses compose to an exact larger delta and remain individually queryable | **Verified locally** | CAP-05 applied `1|4|8` as exact `+13`: `12/12 -> 25/25 -> 12/12`, with distinct status acknowledgements, exact queries, and one percentage restoration per transaction |
| A supported snapshot produces one versioned target and an exact desired-bit registry before mutation | **Verified locally** | HP-PLAN-01 captured `12/12`, calculated target `19` and delta `7`, persisted exactly `4|2|1`, left HP at `12/12`, and removed every plan fact on reset; unsupported native-combat policies created only failure records |
| A planned HP delta applies through exact native acknowledgements and cleans up only recorded bits while preserving percentage once | **Verified locally** | HP-TXN-01 applied `4 -> 2 -> 1`, advanced sum `0 -> 4 -> 6 -> 7`, committed `19/19`, then removed the same three rows and restored `12/12`; commit and cleanup each performed one percentage write |
| An HP commit can trigger one guarded stat/action/bonus component chain and cleanup can remove only recorded component statuses before exact HP cleanup | **Verified locally** | COMPONENTS-01 produced exactly one guarded start and commit, one apply/remove acknowledgement for each owned Stat/Action/Bonus status, reverse component cleanup, exact `4|2|1` HP cleanup, and final `12/12`; no failure row was added |
| The independently authored one-stat-tier status parses, applies, remains queryable, and is removed exactly | **Verified locally** | Exact loaded resource is Attack `+1`, Saves `+1`, AC `+1`, Spell DC `+1`; STAT-01 received apply/remove acknowledgements and active `1 -> 0` for only `AESN_TIER_LEVEL_05_08`. Integrated hostile selection remains RT-08 |
| Enemy classification is existential across all supplied participating snapshot members and duplicate-safe | **Verified locally** | HOSTILITY-01 rejected a neutral-to-both candidate and accepted a candidate neutral to the first but hostile to the second; two consideration calls produced one considered and one eligible fact |
| Native `CombatStarted` and `EnteredCombat` integrate with the immutable snapshot, participant intersection, and duplicate-safe late-enemy dispatch | **Verified locally** | COMBAT-01 created one native narrative combat, one snapshot, one participant, and exactly one consideration/eligibility record for each of an initial and late hostile; repeated late consideration added nothing. Ordinary turn-based encounter behavior remains unverified |
| One fork-owned Action and one Bonus Action status apply additively and are removed exactly | **Verified locally** | ACTION-01 produced distinct apply/remove events, active `1/1` then inactive `0/0`, and touched only `AESN_EXTRA_ACTION_1` and `AESN_EXTRA_BONUS_ACTION_1` |
| Schema-2 production Story and Stats compile in the Toolkit | **Verified locally** | Multiple fresh Toolkit Story rebuilds succeeded with the schema-2 production goals and Stats definitions, including the final clean production set with Relentless enabled and all test/proof goals excluded. |
| The extracted `1.0.0.4` beta package is QA-free and matches the accepted production presentation | **Verified locally** | Package extraction found the production roster hash, schema-2 Story, six Hardened status/icon tiers, Relentless I-II status definitions, all six Relentless atlas images, friendly HP attribution, production metadata, and no forced Tier II/Tier IV QA markers. A final retail level-2 smoke test showed Hardened I with no Relentless. See `evidence/release-candidates/1.0.0.4.md`. |
| `GetActionResourceValuePersonal` reports hostile personal ActionPoint/BonusActionPoint totals at the production allocation point | **Verified locally** | A production narrative-hostile trace established the normal `1/1` allocation-point baseline. The accepted ordinary-NPC trace then proved Relentless I `2/1`, Relentless II `2/2`, cleanup to `1/1` on the following turn refresh, and rejection when a candidate is already above baseline. See `evidence/capability-spikes/ACTION-02-hostile-resource-totals.md`. |
| Versioned Osiris database records for a fully committed application survive a mid-combat retail-game save/load | **Verified locally** | CAP-06 retained the supported schema-1 snapshot, committed HP transaction, exact applied-bit records, component ownership, and unchanged maximum; its observation-only reload verifier reported `doubleApply=0`, and the committed save independently contained the exact fork-owned statuses |
| A save/load persistence proof can safely be executed inside Toolkit Editor Game Mode | **Rejected** | Larian's current Editor navigation guide explicitly warns not to load a savegame in Editor Game Mode because the Toolkit may require a force-quit and restart; CAP-06 therefore requires a separately approved isolated full-game runtime test |
| Active pending state survives a retail mid-combat save/load and is rolled back once by production reconciliation without applying a duplicate bit | **Verified locally** | RT-21 first verified the branch in an editor fixture, then a separately approved retail probe persisted a real schema-1 zero-application `Planned` transaction under hold. On reload production returned `ROLLED_BACK`, removed the transaction, hold, plan queue, bits, and components, retained the original HP values, and emitted `AESN CAP-06 pending reload proof PASSED` with zero duplicate application |
| Inactive stale combat state is detected and exactly cleaned before bookkeeping is retired | **Verified locally** | RT-22 waited for native `CombatEnded`, production observed `CombatIsActive=0`, performed one exact HP cleanup and one percentage restoration, removed the snapshot/transaction/guard/hold, and emitted one PASS with zero AESN status applications |
| Merge event ordering supports pre-cleanup alias creation | **Verified locally** | CAP-07 created two distinct ordinary combats; two native `SwitchedCombat(object,old,new)` events arrived, the discarded owner was marked on the first event, and `CombatEnded(old)` arrived afterward; see `evidence/capability-spikes/CAP-07-merge-order.md` |
| Equal-policy production merge migrates ownership and suppresses discarded cleanup | **Verified locally** | MERGE-01 created one production alias and merged marker before migration, retained the equal survivor policy without a replan, suppressed `CombatEnded(old)`, and removed the alias only after final-owner cleanup; see `evidence/capability-spikes/MERGE-01-equal-production.md` |
| Mismatched-policy production merge independently selects canonical average and size and reconciles every tracked enemy | **Verified locally** | MERGE-02 combined source `average=7,size=1` with source `average=5,size=3`, committed canonical `average=7,size=3`, and reconciled both real transactions with exactly one replan percentage restoration each; see `evidence/capability-spikes/MERGE-02-mismatch-production.md` |
| Multi-hop production merge aliases flatten to the final survivor and defer cleanup to its end | **Verified locally** | MERGE-03 drove two native ordinary-combat merges; production rewrote the first alias directly to the third combat, retained two discarded-to-final markers through both discarded ends, and removed both on the final end; see `evidence/capability-spikes/MERGE-03-alias-chain.md` |
| Host-authoritative Story application produces consistent client observations | **Assumption/unsupported** | RT-26 and RT-27 after separate install approval |

## Evidence boundary

Tactician Enhanced establishes that the broad native mechanism is feasible. No Tactician source, resource definition, localization, UUID, database name, status name, or other authored content may be copied. Every POC identifier and resource is independently authored under the `AESN_` or `DB_AESN_` namespaces.
